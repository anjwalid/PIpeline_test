```json
{
  "threats": [
    {
      "name": "SQL injection",
      "description": "Consiste à injecter des requêtes SQL malveillantes dans un moteur de base de données à travers les entrées utilisateur non sécurisées d’une application (formulaires, URL, cookies, etc.)",
      "attack_scenarios": [
        "Le champ de recherche du document RAG dans le frontend React transmet une requête malveillante via l’API FastAPI, exploitant une concaténation directe dans une requête PostgreSQL utilisant `pgvector`. Une charge utile comme `' OR 1=1; --` contourne les filtres et extrait les métadonnées RH stockées dans la table `employee_documents` sans autorisation explicite.",
        "L’upload d’un fichier PDF via l’API FastAPI déclenche une insertion dans PostgreSQL où le nom du fichier, non sanitisé, est interpolé dans une requête `INSERT`. Un nom de fichier comme `valid.pdf'; DROP TABLE session_tokens; --` supprime la table des jetons Keycloak si les droits du compte technique PostgreSQL sont surélevés, comme dans les configurations par défaut affectées par CVE-2024-24213.",
        "Le paramètre `document_id` de l’API `/api/v1/documents/{id}/metadata`, utilisé pour récupérer les métadonnées via pgvector, est vulnérable à une injection UNION-based. Une requête comme `/api/v1/documents/1%20UNION%20SELECT%20username,password%20FROM%20keycloak_users%20--` expose les identifiants des utilisateurs si le backend FastAPI transmet ce paramètre sans validation à une requête PostgreSQL brute.",
        "Le connecteur JDBC entre FastAPI et PostgreSQL, s’il utilise une version antérieure à la correction de CVE-2024-1597, permet à une requête API légitime modifiée (ex: `{"query": "search"; SHUTDOWN WITH NOWAIT; --"}`) d’exécuter des commandes administratives PostgreSQL via des délégations de rôles mal configurées, perturbant ainsi le service RAG et exposant les logs internes."
      ]
    },
    {
      "name": "Oauth Abuse",
      "description": "Consiste à exploiter des failles dans le flux OAuth pour obtenir des tokens d’accès ou usurper un utilisateur.",
      "attack_scenarios": [
        "Le paramètre `redirect_uri` de la requête d’authentification Keycloak, normalement validé par FastAPI, est contourné via un domaine malveillant en `https://legitime-corporate-domain.evil.com/callback`. Keycloak, dans une configuration affectée par CVE-2023-24456, accepte cette URI et envoie le code d’autorisation au serveur contrôlé par l’attaquant, permettant la récupération du jeton d’un collaborateur RH.",
        "Un jeton Keycloak valide, extrait des logs FastAPI exposés par une mauvaise configuration (ex: niveau `DEBUG` activé en production), est réutilisé dans une requête API vers `/api/v1/rag/query` avec l’entête `Authorization: Bearer <stolen_token>`. Le backend, ne vérifiant pas la revocation des jetons côté Keycloak, exécute la requête RAG avec les droits de la victime, retournant des documents IT internes normalement restreints.",
        "Le flux PKCE, implémenté dans le frontend React, est compromis par une fuite du `code_verifier` via une XSS stockée dans un document uploadé (ex: un PDF avec un script malveillant hébergé sur un objet embarqué). Le code d’autorisation intercepté lors de la redirection post-login permet de générer un jeton Keycloak valide pour le compte de la victime, avec les claims `roles: ["rh-admin", "it-support"]`.",
        "Une application React malveillante, exploitée via une dépendance compromise (scénario similaire à CVE-2025-11953), intercept les requêtes `/oauth/token` légitimes et remplace le `client_id` par celui d’une application Keycloak interne à droits étendus (ex: `internal-rh-tool`). Le jeton obtenu via ce *client_id spoofing* permet ensuite d’appeler l’API `/api/v1/employees/salaries` avec des privilèges élevés."
      ]
    },
    {
      "name": "Broken Object Property Level Authorization",
      "description": "L'API expose des propriétés d'objets sensibles dans ses réponses ou accepte leur modification sans contrôle granulaire au niveau du champ.",
      "attack_scenarios": [
        "La réponse JSON de l’API `/api/v1/documents/{id}` inclut systématiquement le champ `metadata.internal_notes`, destiné aux administrateurs RH, même pour les requêtes des utilisateurs IT. Un collaborateur du support IT, via un appel légitime à cette API, récupère ainsi les annotations confidentielles des dossiers RH (ex: `\"internal_notes\": \"Candidature rejetée pour raison médicale\"`) sans restriction côté FastAPI.",
        "Le endpoint `/api/v1/rag/query` accepte un payload JSON avec un champ `context_options` non documenté, utilisé en interne pour ajuster les paramètres de pgvector. Un utilisateur malveillant soumet `{\"query\": \"politique IT\", \"context_options\": {\"include_private\": true, \"vector_threshold\": -1}}`, forçant le moteur RAG à retourner des documents normalement exclus par les ACLs, comme les comptes-rendus de comités de direction stockés dans PostgreSQL.",
        "Le schéma de validation FastAPI pour le endpoint PATCH `/api/v1/employees/{id}` utilise Pydantic avec `exclude_unset=True`, mais omet de lister explicitement les champs autorisés. Une requête avec `{\"id\": 123, \"salary\": 200000, \"department\": \"rh\", \"is_admin\": true}` met à jour ces propriétés en base via SQLAlchemy, y compris `is_admin`, car le modèle ORM lie automatiquement les champs du payload aux colonnes PostgreSQL sans filtrage au niveau propriété.",
        "Un document uploadé via `/api/v1/documents/upload` génère une entrée dans la table `documents` avec un champ `acl` censé être géré côté serveur. En interceptant la requête, un attaquant ajoute `\"acl\": {\"read\": [\"all\"], \"write\": [\"rh-admin\"]}` au payload JSON, exploitant l’absence de validation stricte dans FastAPI pour surcharger les permissions par défaut et rendre le document accessible à tous les collaborateurs, contournant ainsi les règles métiers implémentées dans Keycloak."
      ]
    }
  ]
}
```